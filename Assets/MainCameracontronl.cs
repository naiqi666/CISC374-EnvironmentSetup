using UnityEngine;

public class MainCameracontronl : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Why do programmers prefer dark mode? Because light attracts bugs!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
